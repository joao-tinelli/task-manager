server {
    listen 80;
    server_name localhost;
    
    root /usr/share/nginx/html;
    index index.html;

    # Qualquer rota que não corresponda a um arquivo estático será redirecionada para index.html (Padrão para React SPAs)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
